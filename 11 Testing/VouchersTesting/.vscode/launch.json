{
    "version": "0.2.0",
    "configurations": [
        {
          "type": "chrome",
          "request": "attach",
          "name": "Attach Karma Chrome",
          "address": "localhost",
          "port": 9876,
          "pathMapping": {
            "/": "${workspaceRoot}",
            "/base/": "${workspaceRoot}/"
          }
        }
      ]
}